class browse : public cardealership {
public:
  void search(){
       cout << "Search results for " <<  << "\n";
       bool found = false;

       for () {
           if (vehicle->getName().find != ) {
              cout << vehicle->getName() << "$" << vehicle->getPrice() << "\n"
                }
       }

       if (!found) {
         cout << "No vehicle matching the search.\n";
       }
  }
